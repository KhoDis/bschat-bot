import { injectable } from 'inversify';

/**
 * Info UI - Rendering for game information and statistics
 * 
 * Currently, most formatting is done in the service layer.
 * This file is prepared for future UI extraction.
 */
@injectable()
export class InfoUi {
  // Future: Move leaderboard formatting, stats formatting here
}

